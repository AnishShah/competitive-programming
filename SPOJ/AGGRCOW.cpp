#include <cstdio>
#include <vector>
#include <algorithm>
using namespace std;
#define REP(i, n) for(int i = 0; i < (n); i++)

int main(){
	int t;
	scanf("%d", &t);
	while(t--){
		int N, C;
		scanf("%d %d", &N, &C);
		vector<int> 
		REP(i, N){
			scanf("%d", &);
		}
	}
}